syntax="proto3";

service Foo{
    rpc Bar(micro-api.api.Request) returns(micro-api.api.Response){};
}

service Example{
    rpc Call(Request) returns(Response){};
}
message Request{
    string name=1;
}

message Response{
    string msg=1;
}